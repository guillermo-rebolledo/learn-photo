# Deploy the MVP to Vercel

The initial hosting target will be Vercel for its Next.js workflow, preview deployments, CDN delivery, and straightforward production releases. The application will retain static-export compatibility and will not depend on Vercel-only runtime features for its core learning, simulator, or Progress behavior, preserving the option to move to another static host.
